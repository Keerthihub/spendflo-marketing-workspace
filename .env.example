# Select one provider: removebg (default) or clipdrop
BACKGROUND_PROVIDER=removebg

# Configure the key for the selected provider. Keep real keys in Vercel
# environment variables; never commit them to this repository.
REMOVE_BG_API_KEY=
CLIPDROP_API_KEY=
