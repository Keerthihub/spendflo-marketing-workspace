# Free Background Remover — No API Keys Required
# Uses the free, open-source rembg library for unlimited processing
#
# Setup:
#   1. Install Python 3.7+ if not already installed
#   2. Run: pip install rembg pillow
#   3. No configuration needed — it just works!
#
# That's it! No API keys, no rate limits, unlimited processing for your team.
